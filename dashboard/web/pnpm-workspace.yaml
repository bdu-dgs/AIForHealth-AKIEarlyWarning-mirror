allowBuilds:
  esbuild: true
  sharp: false
  workerd: false
enableGlobalVirtualStore: false
nodeLinker: hoisted
packageImportMethod: copy

# Keep the cache local to avoid global project-registration symlinks.
storeDir: .pnpm-store
